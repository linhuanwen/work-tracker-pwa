# 桌面启动器（launcher.py）
pywebview>=6.0
pystray>=0.19
Pillow>=10.0
python-docx>=1.0
requests>=2.28

# Qt 桌面替代（main.py / models / screens / services）
PySide6>=6.5.0
pydantic>=2.0

# 打包工具
pyinstaller>=6.0

# AI 润色脚本（scripts/polish.py）
# requests 已在上面声明
